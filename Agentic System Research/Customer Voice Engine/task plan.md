| Task ID   | Phase                    | Task Name                                                           | Description                                                                                                                                                                                                                                                                                                                                                                                                                                      | Predecessor | Milestone                                                         | Resource / Owner                                        |
| --------- | ------------------------ | ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------- | ----------------------------------------------------------------- | ------------------------------------------------------- |
| **CV-01** | Architecture & Contracts | **Define Customer Voice Boundaries and Data Contracts**             | Finalize `W_VOICE` as a **zero-sandbox coordinator**; define fixed workflow `Discovery → [Themes \|\| Sentiment \|\| Needs] → Journey → QA → W_VOICE`; add `CustomerVoiceTask`, `VoiceSpecialistTask`, `VoiceSpecialistResult`, `FeedbackRecord`, `EvidenceSpan`, survey metadata, findings, QA, and final payload contracts. Remove fabricated/default feedback behavior.                                                                       | —           | **Voice architecture and typed contracts frozen**                 | Backend Architect / W_VOICE Owner                       |
| **CV-02** | Agent & LLM Layer        | **Implement W_VOICE and Independent Specialist Profiles**           | Modify `customer_voice.py`; add `profiles.py` and six specialists: `VOICE-DISCOVERY`, `VOICE-THEMES`, `VOICE-SENTIMENT`, `VOICE-NEEDS`, `VOICE-JOURNEY`, `VOICE-QA`. Create **7 isolated LLM identities** with separate prompts, budgets, profile digests, context, permissions, and provenance identity.                                                                                                                                        | CV-01       | **W_VOICE + six specialist identities operational**               | Agent / LLM Engineering                                 |
| **CV-03** | Sandbox & Security       | **Enforce Specialist-Only Fresh AIO Execution**                     | Remove direct `W_VOICE → S_PARSE`; authorize only `VOICE-*` specialists. Implement fresh `SandboxIdentity → provision → execute → seal → destroy` per attempt. Default all specialists to `DENY_ALL`; permit only task/domain-bound `VOICE-DISCOVERY` egress. Add remote `S_PARSE` execution and prohibit production host fallback.                                                                                                              | CV-02       | **Parent sandbox denied; fresh specialist AIO isolation proven**  | Sandbox / Security Engineering                          |
| **CV-04** | Ingestion & Privacy      | **Build Voice Discovery and Sanitization Pipeline**                 | Implement own-brand source capture, record normalization, language detection, deterministic PII redaction, prompt-injection handling, HMAC/opaque record IDs, integrity hashes, deduplication, survey-method metadata preservation, and evidence-span generation. Raw customer text must not reach the W_VOICE parent LLM or downstream agents before sanitization.                                                                              | CV-03       | **Traceable, de-identified immutable Voice corpus produced**      | `VOICE-DISCOVERY` / Data & Privacy Engineering          |
| **CV-05** | Core Analysis            | **Implement Themes, Sentiment, and Needs Analysis**                 | **Themes:** embeddings, clustering, observed share, diagnostics, time-comparable trends. **Sentiment:** aspect-level polarity/emotion, model/version, uncertainty, evidence spans. **Needs:** pains, objections, desired outcomes, customer vocabulary. Keep embeddings, clustering, classifiers, aggregation, and statistics as deterministic/versioned tools. Never treat observed feedback frequency as population prevalence.                | CV-04       | **Three evidence-grounded analysis packs produced**               | `VOICE-THEMES`, `VOICE-SENTIMENT`, `VOICE-NEEDS`        |
| **CV-06** | Synthesis & Assurance    | **Implement Journey Analysis, QA, Provenance, and Final Synthesis** | `VOICE-JOURNEY` performs descriptive channel/touchpoint/segment/time comparisons without causal inference. `VOICE-QA` independently checks residual PII, evidence traceability, contradictions, bias, coverage, schema validity, uncertainty, and representativeness; QA cannot mutate candidate outputs. Record W3C PROV lineage for sources, tools, models, specialists, QA, and synthesis. W_VOICE produces the sanitized `EvidenceEnvelope`. | CV-05       | **QA-approved, provenance-complete Customer Voice payload**       | `VOICE-JOURNEY`, `VOICE-QA`, W_VOICE / Provenance Owner |
| **CV-07** | Verification & Release   | **Integration, Security Tests, Documentation, and Release Gate**    | Test parent sandbox denial, independent LLM identities, fresh AIO runtime per attempt, tenant/sibling isolation, Discovery allowlist, zero egress elsewhere, no host fallback, PII handling, exact evidence spans, survey-bias metadata, uncertainty, unsupported=`not_assessed`, QA immutability, and provenance completeness. Update architecture/docs/flowchart to remove obsolete `W_VOICE ↔ S_PARSE`.                                       | CV-06       | **All acceptance/security tests pass; architecture synchronized** | QA / Security / Backend / Architecture Owners           |

**Execution sequence:** `CV-01 → CV-02 → CV-03 → CV-04 → CV-05 → CV-06 → CV-07`.

Within **CV-05**, `VOICE-THEMES`, `VOICE-SENTIMENT`, and `VOICE-NEEDS` can run in parallel over the same immutable sanitized corpus.

Some earlier uploaded architecture attachments are no longer directly available in this session. This plan is grounded in the current **research findings** attachment, which consolidates their validated conclusions; re-upload the older files if you want the plan rechecked line-by-line against them.
