Based on the attached **research findings** and **IMPLEMENTATION.md**, the essential Strategy Engine plan is:

| Task ID      | Phase                                       | Task Name                                        | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Predecessor | Milestone                              | Resource / Owner                       |
| ------------ | ------------------------------------------- | ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | -------------------------------------- | -------------------------------------- |
| **STRAT-01** | Architecture & Contracts                    | **Finalize W_STRAT / S_ALLOC Boundaries**        | Define and lock the Strategy Engine execution contract. **Subtasks:** confirm `W_STRAT` as Layer-5 strategy/synthesis worker; retain only `S_ALLOC` as the Layer-6 Strategy specialist; define separate purpose-scoped LLM responsibilities; prohibit direct `S_ALLOC → W_STRAT`, RAG, DB, IE-internal, campaign-platform, or external-action access; preserve IE-mediated read-only context and `EvidenceEnvelope` output.                                                                                                                  | —           | **Strategy Architecture Approved**     | Strategy Architect / Backend Architect |
| **STRAT-02** | Worker & Sub-Agent Implementation           | **Implement W_STRAT and S_ALLOC Reasoning**      | Update `strategy.py` and add `strategy_engine/subagents/allocation.py`. **Subtasks:** remove fabricated fallback evidence; enforce tenant/channel/budget scope; pass KPI, telemetry, media history, controls, incrementality evidence, and constraints; implement S_ALLOC’s independent advisory LLM for KPI priorities, scenario emphasis, assumptions, risks, and model interpretation; keep numerical authority outside the LLM.                                                                                                          | STRAT-01    | **Strategy Reasoning Layer Complete**  | Backend / Agent Engineer               |
| **STRAT-03** | Modeling & Optimization                     | **Implement Governed S_ALLOC Toolchain**         | Upgrade deterministic Strategy modeling. **Subtasks:** extend `capabilities.py`; implement `media_mix_modeler`, `budget_allocator_tool`, `funnel_simulator`, and `optimization_modeler`; replace naive ROAS-weighted allocation with constrained diminishing-return/mROI allocation; support channel bounds, saturation, uncertainty, response curves, scenario modeling, model diagnostics, and unallocated contingency; explicitly identify output as a planning proxy when no fitted causal MMM exists.                                   | STRAT-02    | **S_ALLOC Modeling Pipeline Complete** | Marketing Science / Backend Engineer   |
| **STRAT-04** | Sandbox & Runtime Security                  | **Enforce AIO-Sandbox Execution Boundary**       | Make S_ALLOC execute through the hardened sandbox rather than silently inside the backend when AIO Sandbox is configured. **Subtasks:** update `sandbox/client.py`; update `s-alloc/SKILL.md` and `scripts/run.py`; use task-scoped `/workspace`; keep network disabled; deny backend source, sibling sandbox, credentials, RAG/DB/IE and browser access; fail closed when configured remote sandbox execution fails; retain local deterministic fallback only for test/development mode without a sandbox endpoint.                         | STRAT-03    | **Sandbox Isolation Verified**         | Sandbox / Security Engineer            |
| **STRAT-05** | Integration, Validation & Release Readiness | **Integrate, Test and Validate Strategy Engine** | Wire independent W_STRAT/S_ALLOC LLM clients through the appropriate composition/dispatch boundaries and validate the complete IE → W_STRAT → S_ALLOC → sandbox → W_STRAT → `EvidenceEnvelope` → IE/HITL flow. **Subtasks:** extend Strategy unit/integration tests; test mROI/saturation/constraints/diagnostics; verify no fabricated evidence; verify Model-A access; test network denial, capability allowlists, tenant/channel isolation and no reverse dependency; run regression/CI and verify provenance/HITL behavior before merge. | STRAT-04    | **Strategy Engine Release-Ready**      | QA / Backend / Security Engineer       |

The dependency chain is intentionally linear: **Architecture → Reasoning → Modeling → Sandbox Enforcement → Integration & Validation**. This keeps the Strategy Engine minimal and avoids introducing unsupported MMM, funnel, campaign, verifier, database, RAG, or persistence agents.
